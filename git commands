# Navigate to your root project folder
cd path/to/quickaid-first-aid-app

# Step 1: Initialize Git
git init

# Step 2: Add all files to staging
git add .

# Step 3: Commit the files
git commit -m "Initial commit: QuickAid full project with frontend and backend"

# Step 4: Connect to your GitHub repo (create it first at https://github.com/new)
git remote add origin https://github.com/logitechsabarni/quickaid-first-aid-app.git

# Step 5: Rename branch to main (GitHub default)
git branch -M main

# Step 6: Push code to GitHub
git push -u origin main
